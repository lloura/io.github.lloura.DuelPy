app-id: io.github.lloura.DuelPy
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: duelpy

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri

cleanup:
  - /include
  - /lib/pkgconfig
  - /share/pkgconfig
  - /share/man
  - "*.la"
  - "*.a"

modules:
  - name: duelpy
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/lloura/DuelPy.git
        tag: v0.3.4
        commit: db6d3392ab5c8e9ea679882bb1915c2f92f54c3a
