app-id: io.github.lloura.DuelPy
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: duelpy

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri

modules:
  - name: duelpy
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/lloura/DuelPy.git
        tag: v0.3.2
        commit: 1ff453a690572d29564fd73f7b4f162d0d38a259
